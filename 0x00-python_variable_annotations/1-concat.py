#!/usr/bin/env python3
"""type-annotated function concat that takes a string str1 and
a string str2 as arguments and returns a"""


def concat(a: str, b: str) -> str:
    """type-annotated function concat that takes a string str1 and
    a string str2 as arguments and returns a
    Args:
        a: string
        b: string
    Returns:
        string concat a + b
    """
    return a + b
